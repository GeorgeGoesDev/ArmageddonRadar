# NASA NeoWs API key. Get a free one in seconds at https://api.nasa.gov/
# Copy this file to `.env` and drop your key in. Falls back to DEMO_KEY
# (shared, ~30 requests/hour) if unset.
EXPO_PUBLIC_NASA_API_KEY=DEMO_KEY
